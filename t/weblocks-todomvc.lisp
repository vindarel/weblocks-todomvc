(in-package :cl-user)
(defpackage weblocks-todomvc-test
  (:use :cl
        :weblocks-todomvc
        :prove))
(in-package :weblocks-todomvc-test)

;; NOTE: To run this test file, execute `(asdf:test-system :weblocks-todomvc)' in your Lisp.

(plan nil)

;; blah blah blah.

(finalize)
